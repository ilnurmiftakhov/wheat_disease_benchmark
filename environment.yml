name: wheat-disease-benchmark
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - numpy>=1.24
  - pillow>=10.0
  - opencv
  - pytorch>=2.2
  - torchvision>=0.17
  - pip
  - pip:
      - ultralytics>=8.1
